name: Build and Release
on:
  push:
    branches:
      - wael

permissions:
  contents: write
  packages: write

jobs:
  build-and-release:
    runs-on: mac-mini-ios-sh-1
    steps:
    - name: Checkout ${{ github.ref_name }}
      uses: actions/checkout@v4

